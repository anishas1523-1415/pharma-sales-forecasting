# Core data & ML
pandas>=2.0
numpy==1.26.4
scikit-learn>=1.3

# Forecasting models
prophet>=1.1.5          # needs a C++ toolchain to build its Stan backend on some platforms
statsmodels>=0.14
pmdarima>=2.1            # auto_arima order selection for arima_train_eval.py / sarima_train_eval.py --use-auto
lightgbm>=4.0
optuna>=4.0               # hyperparameter tuning for train_lightgbm.py --use-optuna

# Deep learning (optional — for LSTM)
# tensorflow>=2.13        # no wheel yet for Python 3.14 as of writing; use Python <=3.12 for LSTM retraining

# API backend
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0
httpx>=0.27
requests>=2.31

# Dashboard frontend
streamlit>=1.35
plotly>=5.20

# Utilities
joblib>=1.3
pyarrow>=14.0
