VITE_API_BASE_URL=http://localhost:8000

# Only needed if the backend has API_KEYS set (see backend/auth.py).
# Local dev backends typically don't set API_KEYS, so this can stay blank.
VITE_API_KEY=

# Supabase — real user auth + RLS-secured storage for acknowledged
# recommendations (frontend/utils' localStorage-only version is now the
# fallback for unauthenticated/offline use). The anon key is safe to
# expose client-side by design — every table it can touch is protected
# by Row Level Security, not by keeping this key secret.
VITE_SUPABASE_URL=https://sblxjicptjvdifpvysel.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_HmWiApHgQOD-bODojBcpkA_arSc5kd7
